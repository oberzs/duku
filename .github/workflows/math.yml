name: Math test

on:
  push:
    branches: [master]
    paths:
      - "tenge-math/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/cargo@v1
        with:
          command: test
          args: --release --package tegne-math
