[package]
name = "tegne"
version = "0.1.0"
authors = ["Oliver Berzs <oliver.berzs@gmail.com>"]
edition = "2018"

[dependencies]
ash = "0.30.0"
log = "0.4.8"

tegne-utils = { path = "../tegne-utils", version = "0.1.0", optional = true }

[features]
default = ["tegne-utils"]